# --- core: the agent itself -------------------------------------------------
google-genai>=2.3.0
python-dotenv>=1.0.0

# --- terminal HUD -----------------------------------------------------------
rich>=13.7.0

# --- the desktop app ------------------------------------------------------
# Missing from this file until a clean-clone test caught it: the app imports
# both, so without them `python app.py` fails on a fresh machine with
# "customtkinter (the window)" -- which reads as a broken download rather
# than a missing dependency.
customtkinter>=5.2.0
pystray>=0.19.5

# --- eyes -------------------------------------------------------------------
mss>=9.0.1
Pillow>=10.0.0

# --- hands: files, system, desktop ------------------------------------------
psutil>=5.9.0
pyperclip>=1.8.2
pygetwindow>=0.0.9
pyautogui>=0.9.54
pywin32>=306; sys_platform == "win32"

# --- web --------------------------------------------------------------------
httpx>=0.27.0
beautifulsoup4>=4.12.0

# --- ears and mouth (local, free, no Gemini quota) --------------------------
faster-whisper>=1.0.0
sounddevice>=0.4.6
numpy>=1.26.0
piper-tts>=1.2.0
pynput>=1.7.6

# --- nerves: scheduled and event-driven triggers ----------------------------
APScheduler>=3.10.0

# --- google workspace, read-only (optional) ---------------------------------
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# --- only needed to BUILD the shareable .exe -------------------------------
# pip install pyinstaller
